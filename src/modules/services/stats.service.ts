// import { CreateStatsDto } from "../dtos/user.dto";
// import Stats from "../models/stats.model";

// class StatsService {
//   async createStat(body: CreateStatsDto) {
//     try {
//       const createdState = await Stats.create({
        
//       });

//       return createdStat;
//     } catch (error) {
//       throw new Error("No se pudo crear el stats");
//     }
//   }
// }

// export default new StatsService();